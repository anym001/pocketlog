## Version 2026/05/14
# PocketLog – Forward Auth via Authentik (HTTP Basic Auth Modus)
#
# Voraussetzungen:
#   - SWAG Container läuft (linuxserver/swag)
#   - Authentik Outpost ist über SWAG erreichbar (proxy-confs/authentik-*.conf)
#   - DNS-Eintrag pocketlog.<deinedomain> zeigt auf SWAG
#   - PocketLog-Container hängt im gleichen Docker-Netzwerk wie SWAG
#   - Im Authentik-Proxy-Provider ist "HTTP Basic authentication" aktiviert,
#     damit Authentik die Credentials selbst validiert und kein blinder
#     Header-Trust nötig ist.

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;

    server_name pocketlog.*;

    include /config/nginx/ssl.conf;

    client_max_body_size 0;

    # Authentik Outpost (stellt /outpost.goauthentik.io/* bereit)
    include /config/nginx/authentik-server.conf;

    location / {
        # Authentik Forward Auth – validiert Basic-Auth-Credentials gegen
        # Authentik und setzt anschließend die User-Header.
        include /config/nginx/authentik-location.conf;

        include /config/nginx/proxy.conf;
        include /config/nginx/resolver.conf;

        # Basic-Auth-Credentials nicht an den Backend-Container weiterreichen
        # – Authentik hat sie bereits validiert.
        proxy_set_header Authorization "";

        set $upstream_app pocketlog;
        set $upstream_port 8000;
        set $upstream_proto http;
        proxy_pass $upstream_proto://$upstream_app:$upstream_port;

        # Den von Authentik gesetzten Header an FastAPI durchreichen
        proxy_set_header X-Authentik-Username $authentik_username;
    }
}

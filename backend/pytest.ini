[pytest]
# Run from backend/ so `from app import ...` and alembic.ini both resolve.
pythonpath = .
testpaths = tests
filterwarnings =
    # SQLAlchemy 2.0 emits this when fastapi's TestClient closes a thread
    # local session that's been overridden; harmless under our override.
    ignore::DeprecationWarning

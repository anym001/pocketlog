# Secrets / lokale Konfiguration
.env
.env.local

# Lokale Tool-Konfiguration
.claude/settings.local.json
.superpowers/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/

# SQLite dev/test databases (DATABASE_URL=sqlite:///…)
backend/*.db
backend/*.db-journal
backend/*.db-wal
backend/*.db-shm

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
preview.html
preview.db
preview_server.py

# Playwright e2e (UI smoke)
frontend/e2e/node_modules/
frontend/e2e/package-lock.json
frontend/e2e/playwright-report/
frontend/e2e/test-results/

# Vitest unit tests (frontend helpers)
frontend/unit/node_modules/
frontend/unit/package-lock.json

# Root JS tooling (Prettier + ESLint)
/node_modules/
/package-lock.json
frontend/node_modules/

# Local dev: frontend symlink/copy for running uvicorn from backend/
# (the Docker build COPYs the frontend here as /app/static).
backend/static
